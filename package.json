{
  "name": "daxcloud",
  "private": true,
  "packageManager": "npm@11.6.3",
  "workspaces": [
    "apps/web", "apps/api",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.0",
    "turbo": "latest",
    "typescript": "^5.4.0"
  }
}
